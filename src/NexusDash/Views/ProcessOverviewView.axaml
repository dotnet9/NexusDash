<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="using:NexusDash.Views"
             xmlns:vm="using:NexusDash.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="1060"
             d:DesignHeight="560"
             x:Class="NexusDash.Views.ProcessOverviewView"
             x:DataType="vm:ProcessOverviewViewModel">
  <Grid Margin="0,12,0,0"
        ColumnDefinitions="*,*"
        RowDefinitions="*,*"
        ColumnSpacing="12"
        RowSpacing="12">
    <Border Grid.Column="0"
            Grid.Row="0"
            Classes="panel metricPanel">
      <Grid RowDefinitions="Auto,*,Auto,Auto">
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
          <TextBlock Classes="metricLabel" Text="{Binding CpuText}" />
          <TextBlock Grid.Column="1" Classes="metricValue" Text="{Binding CpuUsageText}" />
        </Grid>
        <views:MetricPlotView Grid.Row="1"
                              MinHeight="120"
                              Margin="0,14,0,10"
                              Values="{Binding CpuHistory}"
                              Stroke="{DynamicResource AccentBrush}" />
        <ProgressBar Grid.Row="2"
                          Value="{Binding CpuUsage}"
                          Minimum="0"
                          Maximum="100"
 />
        <TextBlock Grid.Row="3"
                        Classes="metricHint"
                        Text="{Binding TopCpuProcessText}"
                        Margin="0,8,0,0" />
      </Grid>
    </Border>

    <Border Grid.Column="1"
            Grid.Row="0"
            Classes="panel metricPanel">
      <Grid RowDefinitions="Auto,*,Auto,Auto">
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
          <TextBlock Classes="metricLabel" Text="{Binding MemoryText}" />
          <TextBlock Grid.Column="1" Classes="metricValue" Text="{Binding MemoryUsageText}" />
        </Grid>
        <views:MetricPlotView Grid.Row="1"
                              MinHeight="120"
                              Margin="0,14,0,10"
                              Values="{Binding MemoryHistory}"
                              Stroke="{DynamicResource SuccessBrush}" />
        <TextBlock Grid.Row="2"
                        Text="{Binding MemorySummaryText}"
                        Foreground="{DynamicResource SecondaryTextBrush}" />
        <TextBlock Grid.Row="3"
                        Classes="metricHint"
                        Text="{Binding TopMemoryProcessText}"
                        Margin="0,6,0,0" />
      </Grid>
    </Border>

    <Border Grid.Column="0"
            Grid.Row="1"
            Classes="panel metricPanel">
      <Grid RowDefinitions="Auto,*,Auto">
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
          <TextBlock Classes="metricLabel" Text="{Binding DiskText}" />
          <TextBlock Grid.Column="1" Classes="metricValue" Text="{Binding DiskSpeedText}" />
        </Grid>
        <views:MetricPlotView Grid.Row="1"
                              MinHeight="120"
                              Margin="0,14,0,10"
                              Values="{Binding DiskHistory}"
                              Stroke="{DynamicResource WarningBrush}" />
        <TextBlock Grid.Row="2"
                        Classes="metricHint"
                        Text="{Binding TopDiskProcessText}" />
      </Grid>
    </Border>

    <Border Grid.Column="1"
            Grid.Row="1"
            Classes="panel metricPanel">
      <Grid RowDefinitions="Auto,*,Auto">
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
          <TextBlock Classes="metricLabel" Text="{Binding NetworkText}" />
          <TextBlock Grid.Column="1" Classes="metricValue" Text="{Binding NetworkSpeedText}" />
        </Grid>
        <views:MetricPlotView Grid.Row="1"
                              MinHeight="120"
                              Margin="0,14,0,10"
                              Values="{Binding NetworkHistory}"
                              Stroke="{DynamicResource DangerBrush}" />
        <TextBlock Grid.Row="2"
                        Classes="metricHint"
                        Text="{Binding TopNetworkProcessText}" />
      </Grid>
    </Border>
  </Grid>
</UserControl>
